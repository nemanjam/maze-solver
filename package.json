{
  "name": "maze-solver",
  "version": "1.0.0",
  "description": "Test path finding algorithms.",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "ts-node ./src/main.ts",
    "build": "tsc",
    "test": "jest --maxWorkers=1",
    "coverage": "jest --maxWorkers=1 --coverage",
    "watch": "jest --maxWorkers=1 --coverage --watch",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "22.7.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
